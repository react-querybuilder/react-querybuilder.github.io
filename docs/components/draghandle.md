# DragHandle

Renders a `<span>` element containing a drag handle (`"⁞⁞"` by default). Customize the handle symbol via [`translations.dragHandle`](/docs/components/querybuilder.md#translations). Uses `React.forwardRef` to [enable drag-and-drop functionality](/docs/dnd.md).
